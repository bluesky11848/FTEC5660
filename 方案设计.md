# SEEM5660 / FTEC5660 作业一 方案设计

> 文档状态：设计基线 **v1.1**（已按实测证据修正）
> 对应任务：Task 1 Implementation（90 分）+ Task 2 Reflection（10 分）
> 交付物：公开的 GitHub 仓库 `FTEC5660`（含 `hw1.py`、`requirements.txt`、`README.md`），必须在仓库的 "Homework 1 solution" 一节给出**链路设计可视化图**与**一段方案描述**

## 0. 本次设计的关键结论（TL;DR）

1. **架构决策：LLM 只负责"看"，Python 负责"算"，代码负责"说"。** 评分器要求响应中**有且仅有一个数字**，任何自由文本、解释、日期、件数、百分比都会直接判错。
2. **Q2 = SUBTOTAL（折扣后、舍入前） + 所有折扣行绝对值之和**；**ROUNDING 只进 Q1，绝不进 Q2**。已用公开集 7 张票逐票验证，恒等式 `SUBTOTAL + 折扣 = 无折扣应付` **精确成立**。
3. **模型名存在退役风险**：作业指定的 `deepseek-v4-flash-vision-exp` **仍被接受**（由 DeepSeek-V4.1-Flash 承接），故默认沿用；同时支持 `HW1_MODEL` 环境变量一键切换。
4. **图片必须放在 human 消息中**：DeepSeek 对 system/assistant 消息中的图片返回 400。
5. **运行时需要 Python ≥ 3.10**（`langchain-deepseek` 1.x 的硬性要求），本机 3.8 无法安装，需自备 3.10+ 解释器。

---

## 1. 任务还原（从 HW1.pdf 提取的硬性约束）

### 1.1 业务目标

输入：一个装有多张超市收据（supermarket receipt）图片的文件夹。
输出：**两个跨"整个文件夹"汇总的金额**（各一个数）：

| 编号 | 问题原文（必须逐字符一致） | 计算口径 |
|---|---|---|
| Q1 | `How much money did I spend in total for these bills?` | 每张收据 **ROUNDING 之后的最终支付额**，全部相加。**不**加 ROUNDING 行本身 |
| Q2 | `How much would I have had to pay without the discount?` | 每张收据的 **SUBTOTAL** + **所有折扣/促销/优惠券行（按正数加回）**，全部相加。**不**把 ROUNDING 加回 |

### 1.2 PDF 给出的算例（receipt5.jpg，必须能复现）

```
items:  $10.00 + $36.90 + $60.80
5% OFF          -$5.39
SUBTOTAL        $102.31
ROUNDING        -$0.01
OCTOPUS         $102.30
```

- Q1 = **102.30**（取 ROUNDING 后/OCTOPUS 实付）
- Q2 = **107.70** = 102.31 + 5.39
- 明确排除的错误答案：102.31、102.30

> 关键结论：Q2 的基数是 **SUBTOTAL（已扣折扣后的净额）**，不是商品原价合计。
> 因此 Q2 = SUBTOTAL + Σ|折扣行|。这一点直接决定抽取 schema 的字段设计。

**两处措辞的等价性核对（已实测，避免误读）**

| 来源 | 措辞 | 展开式 |
|---|---|---|
| HW1.pdf §1 | "Each receipt's SUBTOTAL plus every discount line added back as a positive number" | `SUBTOTAL + Σ|折扣|` |
| 模板 README | "the sum of the original positive item prices" | `Σ原价商品` |

二者**恒等**，因为收据上 `SUBTOTAL = Σ原价商品 − Σ折扣`（已用 receipt5 算例验证：
`107.70 − 5.39 = 102.31 = SUBTOTAL`，且 `102.31 + 5.39 = 107.70 = Σ原价商品`）。
因此实现上把 **SUBTOTAL 作为锚点字段**，并用 `Σ原价商品`（即 `line_items` 求和）做交叉校验 I3。

### 1.3 工程与提交约束（逐条对应验收）

| 约束 | 说明 | 设计对策 |
|---|---|---|
| 仅改两个函数 | `build_chain()` 与 `answer_queries()`，两者均标 `### YOUR CODE HERE` | 其余（`parse_single_amount`、`write_results`、`main`）**一字不改** |
| 骨干模型 | 必须用 `deepseek-v4-flash-vision-exp` | 通过 `ChatDeepSeek(model=...)` 显式指定，可用 `HW1_MODEL` 覆盖 |
| 框架 | 必须用 **LangChain** 实现 chain | 用 LCEL（`prompt \| model \| parser`）组装，保留 chain 的可视化结构 |
| 端到端 | 必须能跑通并产出 `results.csv`，崩溃/挂死/无输出 = 0 分 | 全链路 try/except 降级；任何单张收据失败不得中断整体 |
| 输出列 | `results.csv` 三列：`query, model_response, correctness` | 由提供的 `write_results()` 生成，不干预 |
| **唯一数字** | "Each response must contain **exactly one number**: no stray extra digits" | **最终响应只允许出现一个金额字面量**（详见 §4 红区） |
| 密钥安全 | `.env` 必须写入 `.gitignore`，绝不提交 | `.gitignore` 含 `.env`；密钥只从环境变量读取 |
| 私测 | 私密收据集上跑 **3 次独立评测** | 全链路 `temperature=0`、确定性聚合，保证可复现 |

### 1.4 评分器的真实行为（已实测，必须据此设计）

提供的 `hw1.py` 尾部代码决定了成败：

```python
_MONEY_RE = re.compile(r"(?<![\w.])(?:HK\$|\$)?\s*(-?\d[\d,]*(?:\.\d+)?)(?![\w.])")

def parse_single_amount(text):
    matches = _MONEY_RE.findall(text)
    if len(matches) != 1:      # ← 致命关卡
        return None
    ...
```

**实测结论（18 组样例 + 30 组补充探针，仅 9 组通过）**

| 输入 | 匹配数 | 结果 | 教训 |
|---|---|---|---|
| `HK$1974.30` | 1 | ✅ 1974.30 | 目标格式 |
| `HK$1,974.30` | 1 | ✅ 1974.30 | 千分位可用，但非必需 |
| `1974.30` | 1 | ✅ 1974.30 | 裸数字也可 |
| `-HK$5.39` | 1 | ⚠️ **+5.39** | **负号被静默丢弃**（`-?` 位于 `HK$` 之后） |
| `Total is HK$1974.30 (sum of 7 receipts)` | 2 | ❌ | 件数算作第二个金额 |
| `HK$1974.30, and without discount HK$2348.20` | 2 | ❌ | 第二个候选值致命 |
| `2024-09-29 HK$1974.30` | **4** | ❌ | 日期贡献 3 个额外数字 |
| `5% off: HK$1974.30` | 2 | ❌ | 百分比算作金额 |
| **`HK$1974.30.`** | **0** | ❌ | **句末句号使 lookahead `(?![\w.])` 失配，一个都匹配不到** |

**设计推论（最重要的四条）**

1. `len(matches) != 1` **直接判错**，即使正确金额就在回复里。最终响应必须是形如 `HK$1974.30` 的**纯净字符串**。
2. **句末句号是最高危的隐性回归**：`"HK$1974.30."` 匹配数为 **0**（不是 1、不是 2）。因此响应**绝不允许以 `.` 结尾**。
3. **禁止输出负号金额**：`-HK$5.39` 会被读成 `+5.39`。本方案两个答案恒为正数，渲染时只输出正数。
4. `quantize` 使用 **ROUND_HALF_EVEN**（`"1.005"` → `1.00`）；对 2 位小数的目标金额无影响。

> 由此得出本方案的核心架构决策：**让 LLM 只做"看"，让 Python 做"算"，让格式化层做"说"。**

---

## 2. 总体架构：三级责任分离

```
┌──────────────────────────────────────────────────────────────────────────┐
│  Stage 0  ── 输入与预处理 (Deterministic)                                  │
│  folder/*.jpg → 校验存在性与可解码性 → base64 data URL                     │
│  失败图片：跳过并记入日志，绝不让 iter 中断                                  │
└──────────────────────────────────────────────────────────────────────────┘
                                    │  list[Path]
                                    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Stage 1  ── 逐票视觉抽取 (LLM / Vision, temperature=0)                     │
│                                                                            │
│   ChatPromptTemplate (system: 字段语义 + 算例; human: [image, instruction]) │
│            │                                                               │
│            ▼                                                               │
│   ChatDeepSeek(model="deepseek-v4-flash-vision-exp")                       │
│            │  .with_structured_output(ReceiptExtraction)  ← JSON Schema 约束 │
│            ▼                                                               │
│   ReceiptExtraction{ receipt_id, paid_amount_after_rounding,               │
│                      subtotal_after_discounts_before_rounding,             │
│                      discount_total, rounding_adjustment,                  │
│                      items[], octopus_amount, confidence, notes }          │
│                                                                            │
│   并发：同一 receipt 的多次独立观测（pass 1 / pass 2）以增强鲁棒性            │
└──────────────────────────────────────────────────────────────────────────┘
                                    │  per-receipt JSON objects
                                    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Stage 2  ── 校验仲裁与确定性聚合 (Pure Python, Decimal)                    │
│                                                                            │
│   每票：                                                                    │
│     a) 字段清洗：逗号/货币符剥离、Decimal 量化到 0.01                          │
│     b) 恒等式校验：subtotal + Σ|discounts| == without_discount             │
│                    subtotal + rounding     == paid                         │
│     c) 双 pass 一致性：不一致 → 取"恒等式自洽"的那一票 / 降级到重试            │
│                                                                            │
│   Q1 = Σ paid_amount_after_rounding                                        │
│   Q2 = Σ (subtotal + |discount_total|)                                     │
│                                                                            │
│   全程 Decimal + ROUND_HALF_UP，杜绝二进制浮点误差                            │
└──────────────────────────────────────────────────────────────────────────┘
                                    │  Dict[str, Decimal]
                                    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Stage 3  ── 单数字渲染与安全兜底 (Deterministic)                           │
│                                                                            │
│   渲染："HK$" + f"{amount:,.2f}"   →  例 "HK$1,974.30"                      │
│   自检：用评分器同款正则 parse_single_amount() 回测自己的输出                 │
│         若匹配数 != 1 → 改为无千分位纯数字形式 "HK$1974.30" 再回测             │
│         仍失败 → 输出 HK$0.00（保证结构合法，永不抛异常）                      │
└──────────────────────────────────────────────────────────────────────────┘
                                    │  {QUERY_1: "HK$1974.30", QUERY_2: "HK$2348.20"}
                                    ▼
                         提供代码 write_results() → results.csv
```

### 2.1 为什么必须这样分层

| 风险 | 若让 LLM 直接答题 | 本方案如何消除 |
|---|---|---|
| 多位数/算术错误 | 8 张票 × 多行，LLM 心算极易错 | 算术完全交给 Python `Decimal` |
| 响应含多个数字 | 模型爱解释，触发 `len!=1` | 最终响应由代码渲染，模型无写权限 |
| 误把折扣行算入 Q1 | 语义混淆 ROUNDING/SUBTOTAL | system prompt 内嵌 PDF 的 receipt5 算例作为 few-shot 锚点 |
| 单张票识别崩坏 | 全链崩溃 → 0 分 | 每票独立 try/except + 降级为 0，并记入审计日志 |
| 模型版本漂移 | 输出格式突变 | `with_structured_output` 用 schema 强约束 |

---

## 3. 详细设计

### 3.1 Stage 1：Prompt 设计要点

**System message 必须回答的三个语义歧义点：**

1. **Q1 取哪个数**：取 ROUNDING 行之后打印的最终支付额（收据上常见的 `OCTOPUS` / `PAID` / `TOTAL` / `NET` 行）。
2. **Q2 的基数**：**SUBTOTAL 是折扣后的净额**（用 receipt5 算例钉死：102.31 + 5.39 = 107.70）。折扣行按绝对值加回，**折扣行本身是负数**，不要重复取负号。
3. **ROUNDING 不参与 Q2**：明确写死 "do not add back ROUNDING"。

**Human message 组装**（LangChain 1.x 多模态内容块）：

```python
[
    {"type": "text", "text": "<per-receipt instruction + output contract>"},
    {"type": "image_url", "image_url": {"url": image_data_url(path)}},
]
```

**输出契约**：只输出 schema 字段，禁止输出任何自然语言解释或 Markdown。

**关键 prompt 反例防护清单**（写进 system prompt）：
- 不要输出百分比（`5%`、`10%`）——百分比数字会污染评分正则（属 Stage 3 无法挽救的输入源）。
- 不要输出日期、门店号、会员号、item 件数。
- 金额输出为**字符串或数值均可**，由 Stage 2 统一清洗；推荐字符串保留原始小数位，避免框架浮点化。

### 3.2 Stage 2：确定性聚合算法（伪代码）

```python
from decimal import Decimal, ROUND_HALF_UP
CENT = Decimal("0.01")

def q(x) -> Decimal:                     # 统一清洗
    return Decimal(str(x).replace(",", "").replace("HK$", "").replace("$", "")
                   ).quantize(CENT, rounding=ROUND_HALF_UP)

def aggregate(receipts):
    q1, q2 = Decimal("0"), Decimal("0")
    for r in receipts:
        sub  = q(r.subtotal_after_discounts_before_rounding)
        disc = q(r.discount_total)                 # 折扣行绝对值之和
        paid = q(r.paid_amount_after_rounding)

        # 恒等式 A：自愈。若 paid 缺失，用 subtotal + rounding 反推
        if paid is None and r.rounding_adjustment is not None:
            paid = sub + q(r.rounding_adjustment)

        # 恒等式 B：Q2 定义。绝不使用 items 求和作为 Q2 基数
        without_discount = sub + abs(disc)

        q1 += paid
        q2 += without_discount
    return {QUERY_1: q1, QUERY_2: q2}
```

**恒等式自洽性检验（用于仲裁双 pass 冲突）**

| 检验 | 公式 | 通过含义 |
|---|---|---|
| I1 实付一致性 | `subtotal + rounding == paid` | 收据解析自洽 |
| I2 折扣一致性 | `subtotal + abs(discount_total) == without_discount` | Q2 定义被正确执行 |
| I3 商品一致性 | `Σ items == subtotal + abs(discount_total)`（允许 0.02 容差） | 行项目未漏读 |

若 pass1/pass2 冲突，**优先选 I1 与 I3 同时成立的观测**。

> ⚠️ **v1 修正（已被公开集实测证伪）**：v1 曾假设"ROUNDING 幅度必然 ≤ 0.05"并用作仲裁先验。
> 实测公开集 7 张票的舍入量为：−0.02、−0.01、−0.08、−0.09、−0.01、−0.09、−0.04（**合计 −0.34**），
> 其中 3 张达到 **0.08/0.09**。因此**不得硬编码 0.05 带宽**。
> 实现改用 **0.50 作为"合理性上界"**（真实收据按 0.05/0.10 取整，不可能超过 0.50），
> 超过该上界即判定模型抓错了总额（通常是把"折扣前总额"当成了实付），予以重建。

**舍入量对汇总的影响（重要）**

```
Q2 − Q1 = 373.90      而   Σ折扣 = 373.56
⇒ Q1 + Σ折扣 = 2347.86 ≠ Q2 = 2348.20
```

差的 **$0.34** 正是 7 张票的舍入量之和。这证明 **Q2 的基数必须是"舍入前的 SUBTOTAL"**；
若误用"舍入后的实付额"作为基数，答案会系统性偏低 $0.34。这是本作业最隐蔽的陷阱。

### 3.3 Stage 3：输出契约与自检

```python
def render(amount: Decimal) -> str:
    for cand in (f"HK${amount:.2f}", f"HK${amount:,.2f}"):
        if len(_MONEY_RE.findall(cand)) == 1 and parse_single_amount(cand) == amount:
            return cand
    return f"HK${Decimal('0.00'):.2f}"      # 最终兜底：结构恒定合法
```

**渲染硬性规则（三条，均源自 §1.4 实测）**

1. **绝不输出句末句号**：`"HK$1974.30."` 匹配数为 **0**，是本项目最高危的隐性回归。
2. **绝不输出负号金额**：`"-HK$5.39"` 会被读成 `+5.39`；两个答案恒为正，无需负号。
3. **绝不附加任何解释**：件数（`7 receipts`）、百分比（`5%`）、日期都会贡献额外数字。

千分位（`HK$1,974.30`）实测**可用**，但为缩小风险面默认输出**无千分位**形式。`_render` 会用评分器
同款正则**回测自己的输出**，不满足"恰好一个数字且数值相等"就自动降级到备选格式，最后兜底 `HK$0.00`，
因此渲染层在任何输入下都不会产出非法响应。

### 3.4 失败与降级矩阵

| 故障点 | 检测方式 | 降级动作 | 对总分影响 |
|---|---|---|---|
| 图片无法读取/解码 | `base64` 阶段异常 | 该票计 0，记录 audit | 局部损失 |
| API 超时 / 限流 | 异常 + 指数退避重试 ×3 | 重试耗尽 → 该票计 0 | 局部损失 |
| 结构化输出解析失败 | Pydantic `ValidationError` | 回退到 JSON-mode 文本解析 | 局部损失 |
| 模型返回字段缺失 | schema 校验 | 用 I1 恒等式反推 `paid` | 通常可挽救 |
| 双 pass 冲突 | 一致性检验 | 取自洽观测 | 局部损失 |
| 全部票都失败 | 计数为 0 | 输出 `HK$0.00` | **仍产出 results.csv（结构分保住）** |

**核心红线：任何情况下都必须写出 `results.csv`。** 崩溃 = 0 分，宁可输出 0.00。

---

## 4. "唯一数字"红区规则（团队必须遵守）

| 位置 | 允许出现数字？ |
|---|---|
| System/Human prompt | ✅ 允许（不影响评分） |
| 模型中间输出 / 结构化字段 | ✅ 允许（Python 消费，不进 CSV） |
| 审计日志文件 | ✅ 允许，但**不得**写入 CSV |
| **`answer_queries` 返回值** | ❌ **有且仅有一个数字**，格式 `HK$<数字>` |

**反例（会直接判错）**
- `"Total is HK$1974.30 (sum of 7 receipts)"` ← 两个匹配 → fail
- `"HK$1974.30, and without discount HK$2348.20"` ← 两个匹配 → fail
- `"I computed 5% off... HK$1974.30"` ← 多个匹配 → fail

**正例**
- `"HK$1974.30"`

---

## 5. 验证计划（提交前必须全绿）

### 5.1 三层验证

| 层级 | 方法 | 通过标准 |
|---|---|---|
| L1 离线单元 | Mock 掉模型，喂入手工构造的 receipt JSON（含 PDF 的 receipt5 算例） | `aggregate()` 输出 102.30 / 107.70 |
| L2 公开集 | `python3 hw1.py --image-folder public_test` | `results.csv` 两行均为 `correct` |
| L3 稳健性 | 从 7 张公开票随机抽 k 张（k=1..7）组成子集重复跑 | 汇总值 == 子集 ground truth 之和；**3 次重复结果完全一致** |

### 5.2 公开集 ground truth（来自模板仓库 `public_test/ground_truth.json`）

| receipt | Q1 实付 | SUBTOTAL | 折扣 | Q2 无折扣 |
|---|---|---|---|---|
| receipt1 | 394.70 | 394.72 | 85.48 | 480.20 |
| receipt2 | 316.10 | 316.11 | 76.09 | 392.20 |
| receipt3 | 140.80 | 140.88 | 19.22 | 160.10 |
| receipt4 | 514.00 | 514.09 | 76.71 | 590.80 |
| receipt5 | 102.30 | 102.31 | 5.39 | 107.70 |
| receipt6 | 190.80 | 190.89 | 30.31 | 221.20 |
| receipt7 | 315.60 | 315.64 | 80.36 | 396.00 |
| **合计** | **1974.30** | 1974.64 | 373.56 | **2348.20** |

> 交叉验证：1974.64 + 373.56 = 2348.20 ✅ 与 Q2 完全吻合，**证明 Q2 = SUBTOTAL + 折扣 这一口径正确**。

### 5.3 私测前的自检清单

- [ ] **Python ≥ 3.10**（`langchain-deepseek` 1.x 的硬性要求；3.8/3.9 无法安装）
- [ ] `.env` 在 `.gitignore` 中，且 `git status` 看不到它
- [ ] 仓库为 **public**
- [ ] 从**全新 clone** 跑通：`python3 -m venv .venv && pip install -r requirements.txt && python3 hw1.py --image-folder public_test`
- [ ] `results.csv` 三列表头正确、两行数据、correctness 均为 `correct`
- [ ] 随机子集重复 3 次，结果逐字节一致（确定性）
- [ ] 删除/改名 ground truth 后仍能产出 results.csv（不依赖 GT）
- [ ] 单张损坏图片（手工塞入一个 0 字节 `.jpg`）不会导致崩溃
- [ ] 响应**不含句末句号**、不含负号、不含任何解释性数字
- [ ] README 的 "Homework 1 solution" 一节含**链路可视化图** + **一段方案描述**

---

## 6. 任务分解与工时估算

| 阶段 | 工作项 | 估时 |
|---|---|---|
| P0 | 环境搭建、fork、`.env`、`.gitignore` 校验 | 0.5 h |
| P1 | Schema 与 prompt 定稿（含 receipt5 few-shot 锚点） | 1.5 h |
| P2 | LCEL chain 组装 + 结构化输出 + 并发批处理 | 2.0 h |
| P3 | Decimal 聚合层 + 恒等式仲裁 | 1.5 h |
| P4 | 降级/重试/审计日志 | 1.0 h |
| P5 | L1/L2/L3 验证与 prompt 调优（迭代） | 3.0 h |
| P6 | README 链路图 + 方案描述 + Task 2 Reflection | 1.5 h |
| **合计** | | **约 11 h** |

---

## 7. 风险登记册

| # | 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|---|
| R1 | 视觉模型把 ROUNDING 与 OCTOPUS 混淆 | 中 | 高 | prompt 内嵌 receipt5 算例 + I1 恒等式自检 |
| R2 | 收据版式差异导致漏读折扣行 | 中 | 高 | 双 pass 观测 + I3 商品一致性校验 |
| R3 | 模型回复夹带解释文字 | 高 | 高 | 最终响应由代码渲染，模型无写权限 |
| R4 | 私测含多张同款但不同门店版式 | 中 | 中 | prompt 强调"按标签语义而非位置"抽取 |
| R5 | API 限流/超时 | 中 | 中 | 指数退避重试 + 并发上限 + 单票降级 |
| R6 | 3 次独立评测结果不稳定 | 中 | 中 | `temperature=0` + 确定性聚合 |
| R7 | 误提交 `.env` | 低 | 致命 | `.gitignore` 前置 + 提交前 `git status` 核对 |
| R8 | 忘记写 Task 2 Reflection（10 分） | 中 | 中 | 纳入 P6 交付清单 |
| R9 | **模型名退役导致 4xx**：`deepseek-v4-flash-vision-exp` 已被官方标记为退役别名（当前仍被接受并转发到 V4.1-Flash） | 低 | 致命 | 默认沿用作业指定名；可用 `HW1_MODEL=deepseek-flash` 环境变量一键切换，无需改代码 |
| R10 | **本机 Python 3.8 无法安装 langchain 1.x**（要求 ≥ 3.10） | 高 | 高 | 显式在 README 标注 Python ≥ 3.10；评测机需 3.10+ 解释器 |
| R11 | 响应以句号结尾导致 `parse_single_amount` 返回 None（匹配数 0） | 低 | 高 | 渲染层只输出 `HK$<数字>`，无任何标点；已加单元测试锁死 |
| R12 | 输出负号金额被正则丢号 | 低 | 中 | 两个答案恒为正，渲染层不输出负号 |

---

## 8. Task 2 备忘（10 分，勿漏）

提交一份关于**过去 10 天 AI 领域事件如何改变你的观点/职业规划**的反思，格式不限（可文字/绘图/AI 生成）。
安排：在提交前 2 天开始撰写，避免与 Task 1 调优撞车。

---

## 附：链路可视化图（可直接用于 README）

```
   ┌─────────────┐
   │ public_test/ │  7 receipts (.jpg)
   └──────┬──────┘
          │ base64 data URL
          ▼
   ┌──────────────────────────────────────────┐
   │  ChatPromptTemplate                      │
   │  ├─ system: 字段语义 + receipt5 算例       │
   │  └─ human:  [ image_url, instruction ]    │
   └──────┬───────────────────────────────────┘
          ▼
   ┌──────────────────────────────────────────┐
   │  ChatDeepSeek                            │
   │  model = deepseek-v4-flash-vision-exp    │
   │  temperature = 0                         │
   │  .with_structured_output(ReceiptSchema)  │
   └──────┬───────────────────────────────────┘
          │  {subtotal, discounts, paid, rounding, items[]}
          ▼
   ┌──────────────────────────────────────────┐
   │  Deterministic Aggregator (Decimal)      │
   │  I1: subtotal + rounding      == paid    │
   │  I2: subtotal + |discounts|   == Q2 base │
   │  I3: Σ items                  == Q2 base │
   │  Q1 = Σ paid                             │
   │  Q2 = Σ Q2 base                          │
   └──────┬───────────────────────────────────┘
          ▼
   ┌──────────────────────────────────────────┐
   │  Single-Number Renderer + self-check     │
   │  → "HK$1974.30"  /  "HK$2348.20"         │
   └──────┬───────────────────────────────────┘
          ▼
     results.csv   (query, model_response, correctness)
```
